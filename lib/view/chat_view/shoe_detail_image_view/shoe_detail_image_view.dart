

import 'package:flutter/material.dart';

class ShowDetailImageView extends StatelessWidget {
  const ShowDetailImageView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
